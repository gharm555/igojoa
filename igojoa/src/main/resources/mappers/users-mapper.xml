<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itwill.igojoa.repository.UsersDao">
	<!-- 대현이 짠 쿼리 start -->
	<insert id="create" parameterType="Users">
		INSERT INTO users (userId,
		password, email, phonenumber, nickname,
		userProfileName,
		userProfileUrl) VALUES (#{userId}, SHA2(#{password},
		256), #{email},
		#{phoneNumber}, #{nickName}, #{userProfileName},
		#{userProfileUrl})
	</insert>
	<select id="selectByUserId" resultType="Users"
		parameterType="String">
		SELECT * FROM users WHERE USERID = #{userId}
	</select>
	<select id="selectByIdAndPassword" resultType="Users"
		parameterType="Users">
		SELECT * FROM users WHERE USERID = #{userId} AND
		PASSWORD =
		SHA2(#{password}, 256)
	</select>
	<!-- 대현이 짠 쿼리 finish -->

	<!-- 상원이 짠 쿼리 start -->
	<select id="getUserInfo" resultType="Users" parameterType="String">
		SELECT *
		FROM users
		WHERE userId = #{userId}
	</select>
	
	<!-- 삭제 필요? 2024.06.26 -->
	<!-- <select id="getUserInfo" resultType="UsersInfoDto" parameterType="String">
		SELECT users.userId, users.email, users.phoneNumber, points.currentPoints, points.cumulativePoint
		FROM users
			JOIN points ON users.userId = points.userId
		WHERE users.userId = #{userId}
	</select> -->
	<!-- 상원이 짠 쿼리 finish -->
</mapper>